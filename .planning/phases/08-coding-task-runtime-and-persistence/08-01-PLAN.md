---
phase: 08-coding-task-runtime-and-persistence
plan: "01"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - "src/swarmline/orchestration/coding_task_runtime.py"
  - "src/swarmline/orchestration/default_coding_task_runtime.py"
  - "src/swarmline/session/task_session_store.py"
  - "src/swarmline/todo/tools.py"
  - "src/swarmline/runtime/thin/coding_profile.py"
  - "src/swarmline/runtime/thin/coding_toolpack.py"
  - "src/swarmline/runtime/thin/runtime.py"
  - "tests/unit/test_coding_profile.py"
  - "tests/unit/test_coding_toolpack.py"
  - "tests/unit/test_coding_task_runtime_contract.py"
  - "tests/unit/test_coding_task_runtime.py"
  - "tests/integration/test_coding_task_persistence.py"
  - "tests/integration/test_thin_coding_profile.py"
autonomous: true
requirements:
  - "CTSK-01"
  - "CTSK-02"
  - "CTSK-03"
  - "CTSK-04"
  - "CTSK-05"
must_haves:
  truths:
    - "Coding task lifecycle is mediated by one facade on top of GraphTaskBoard rather than a parallel task engine."
    - "Provider-backed todo persistence and session-to-task binding are part of the active coding runtime path."
    - "Typed snapshot and status mapping semantics survive restart/resume."
    - "Missing provider, missing binding, or unsupported resume paths fail fast."
  artifacts:
    - path: src/swarmline/orchestration/coding_task_runtime.py
      provides: ISP-compliant coding task ports and typed snapshot boundary
      contains: CodingTaskLifecyclePort
    - path: src/swarmline/orchestration/default_coding_task_runtime.py
      provides: concrete coding task facade on top of GraphTaskBoard + TaskSessionStore
      contains: DefaultCodingTaskRuntime
    - path: tests/unit/test_coding_task_runtime_contract.py
      provides: contract-first proof for CodingTaskRuntime operation surface
      contains: test_coding_task_runtime_contract_
    - path: tests/integration/test_coding_task_persistence.py
      provides: restart/resume and roundtrip proof
      contains: test_coding_task_state_survives_restart_resume
    - path: src/swarmline/runtime/thin/coding_toolpack.py
      provides: active coding-path wiring to persistent task/todo executors
      contains: build_coding_toolpack
    - path: src/swarmline/runtime/thin/coding_profile.py
      provides: coding-mode allow-list expansion for persistent task/todo tools
      contains: allowed_tool_names
  key_links:
    - from: src/swarmline/orchestration/coding_task_runtime.py
      to: src/swarmline/multi_agent/graph_task_board.py
      via: facade delegates lifecycle to graph backend
      pattern: GraphTaskBoard|task_board
    - from: src/swarmline/session/task_session_store.py
      to: src/swarmline/orchestration/coding_task_runtime.py
      via: typed snapshot or session binding retrieval
      pattern: session.*task|snapshot
    - from: src/swarmline/runtime/thin/coding_toolpack.py
      to: src/swarmline/runtime/thin/runtime.py
      via: coding-mode executors include persistent task/todo path
      pattern: coding_task|todo_read|todo_write
    - from: src/swarmline/runtime/thin/coding_profile.py
      to: src/swarmline/runtime/thin/coding_toolpack.py
      via: visible tool surface and executable allow-list expand together for persistent task/todo tools
      pattern: todo_read|todo_write|task
---

# Plan 08-01: Coding Task Runtime Contract, Facade, and Persistent Task/Todo/Session Adapters

## Goal

Построить один persistent coding-task runtime path поверх `GraphTaskBoard`, `TaskSessionStore` и provider-backed todo tools, без markdown shims и без lifecycle duplication.

## Tasks

<task id="8.1.1" type="auto" tdd="true" critical="yes">
<name>Contract-first seam и RED tests для полного task lifecycle, resume и persistence roundtrip</name>
<files>src/swarmline/orchestration/coding_task_runtime.py, tests/unit/test_coding_profile.py, tests/unit/test_coding_task_runtime_contract.py, tests/unit/test_coding_task_runtime.py, tests/integration/test_coding_task_persistence.py, tests/integration/test_thin_coding_profile.py</files>
<read_first>
  .planning/phases/08-coding-task-runtime-and-persistence/08-CONTEXT.md
  .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
  src/swarmline/multi_agent/graph_task_board.py
  src/swarmline/session/task_session_store.py
  src/swarmline/protocols/graph_task.py
  src/swarmline/todo/tools.py
  src/swarmline/runtime/thin/coding_toolpack.py
  src/swarmline/runtime/thin/runtime.py
  tests/unit/test_graph_task_board_contract.py
  tests/unit/test_task_session_store_contract.py
</read_first>
<acceptance_criteria>
  `src/swarmline/orchestration/coding_task_runtime.py` exists and contains ISP-compliant ports such as `@runtime_checkable class CodingTaskLifecyclePort(Protocol)`, `CodingTaskReadinessPort(Protocol)`, and `CodingTaskResumePort(Protocol)`.
  `tests/unit/test_coding_task_runtime_contract.py` exists and contains contract tests for the composite lifecycle surface: `create_task`, `list_ready_tasks`, `mark_in_progress`, `mark_blocked`, `unblock_task`, `mark_completed`, `mark_failed`, `cancel_task`, and `resume_task`.
  No single new Protocol in `src/swarmline/orchestration/coding_task_runtime.py` exceeds 5 methods.
  `tests/unit/test_coding_task_runtime.py` exists and contains `def test_coding_task_runtime_invalid_transition_fails`.
  `tests/unit/test_coding_task_runtime.py` contains `def test_coding_task_status_mapping_matches_contract`.
  `tests/unit/test_coding_task_runtime.py` contains tests for `create/list_ready/in_progress/blocked/unblock/completed/failed/cancelled/resume`.
  `tests/integration/test_coding_task_persistence.py` exists and contains `def test_coding_task_state_survives_restart_resume`.
  `tests/integration/test_coding_task_persistence.py` contains `def test_typed_snapshot_roundtrip_preserves_semantics`.
  `tests/integration/test_coding_task_persistence.py` contains `def test_missing_provider_or_binding_fails_fast`.
  `tests/unit/test_coding_profile.py` or `tests/integration/test_thin_coding_profile.py` contains proof that the Phase-07 coding allow-list expands together with persistent `todo_read`, `todo_write`, and task-facing tool exposure.
  `tests/integration/test_coding_task_persistence.py` contains proof that the active coding path uses the persistent runtime facade rather than the thin shim path.
  `python -m pytest tests/unit/test_coding_task_runtime_contract.py tests/unit/test_coding_task_runtime.py tests/integration/test_coding_task_persistence.py -v` exits non-zero before implementation and exits 0 after implementation.
</acceptance_criteria>
<action>
  1. Add `src/swarmline/orchestration/coding_task_runtime.py` as the contract-first seam containing:
     - ISP-compliant small ports, for example `CodingTaskLifecyclePort`, `CodingTaskReadinessPort`, and `CodingTaskResumePort`;
     - typed snapshot dataclasses/types needed for resume-friendly roundtrip.
     Together these ports must cover: `create_task`, `list_ready_tasks`, `mark_in_progress`, `mark_blocked(reason)`, `unblock_task`, `mark_completed`, `mark_failed(reason)`, `cancel_task`, `resume_task`, but no single new Protocol may exceed 5 methods.
  2. Create `tests/unit/test_coding_task_runtime_contract.py` with RED-first contract tests proving any valid `CodingTaskRuntime` implementation satisfies the required operation surface and fail-fast semantics.
  3. Create `tests/unit/test_coding_task_runtime.py` with RED-first behavior tests for:
     - one explicit runtime owner mediates task state changes;
     - supported task statuses `pending`, `ready`, `in_progress`, `blocked`, `completed`, `failed`, `cancelled` are represented through the coding-runtime contract even if the underlying graph backend uses fewer native statuses;
     - invalid status transitions fail;
     - the full lifecycle surface is usable end-to-end: create, list-ready, in-progress, blocked, unblock, completed, failed, cancelled, resume.
  4. Create `tests/integration/test_coding_task_persistence.py` with RED-first tests for:
     - task state survives restart/resume in supported persistence mode;
     - session-to-task binding survives restart/resume;
     - provider-backed todo state survives restart/resume;
     - typed snapshot roundtrip preserves semantic state;
     - typed snapshot is persisted on create/transition and resume rehydrates snapshot semantics before status projection for derived statuses like `pending`, `ready`, and `failed`;
     - missing provider or missing binding raises explicit failure;
     - coding-mode task/todo calls go through the persistent runtime facade/executor path, not `runtime/thin/builtin_tools.py` shim behavior.
  5. Add RED-first policy/tool-surface proof in `tests/unit/test_coding_profile.py` or `tests/integration/test_thin_coding_profile.py` that Phase 07's allow-list/profile contract expands in lockstep with persistent `todo_read`, `todo_write`, and task-facing coding tools.
</action>
<verify>
  <automated>python -m pytest tests/unit/test_coding_profile.py tests/unit/test_coding_task_runtime_contract.py tests/unit/test_coding_task_runtime.py tests/integration/test_coding_task_persistence.py tests/integration/test_thin_coding_profile.py -v</automated>
</verify>
<done>
  Contract-first task runtime ports exist before any concrete implementation is added, and no single new Protocol exceeds the project ISP limit.
  RED tests exist for the full lifecycle surface, typed snapshots, restart/resume, and explicit failure paths.
  The failing assertions point to missing runtime/persistence implementation rather than missing imports or syntax errors.
</done>
</task>

<task id="8.1.2" type="auto" tdd="true" critical="yes" depends="8.1.1">
<name>Implement coding task runtime facade, typed snapshot boundary, and active coding-path wiring</name>
<files>src/swarmline/orchestration/coding_task_runtime.py, src/swarmline/orchestration/default_coding_task_runtime.py, src/swarmline/session/task_session_store.py, src/swarmline/todo/tools.py, src/swarmline/runtime/thin/coding_profile.py, src/swarmline/runtime/thin/coding_toolpack.py, src/swarmline/runtime/thin/runtime.py, tests/unit/test_coding_profile.py, tests/unit/test_coding_toolpack.py, tests/unit/test_coding_task_runtime_contract.py, tests/unit/test_coding_task_runtime.py, tests/integration/test_coding_task_persistence.py, tests/integration/test_thin_coding_profile.py</files>
<read_first>
  src/swarmline/orchestration/coding_task_runtime.py
  src/swarmline/multi_agent/graph_task_board.py
  src/swarmline/protocols/graph_task.py
  src/swarmline/session/task_session_store.py
  src/swarmline/todo/tools.py
  src/swarmline/runtime/thin/coding_profile.py
  src/swarmline/runtime/thin/coding_toolpack.py
  src/swarmline/runtime/thin/runtime.py
  .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
  tests/unit/test_coding_profile.py
  tests/unit/test_coding_toolpack.py
  tests/unit/test_coding_task_runtime_contract.py
</read_first>
<acceptance_criteria>
  `src/swarmline/orchestration/default_coding_task_runtime.py` exists and contains `DefaultCodingTaskRuntime`.
  `src/swarmline/orchestration/coding_task_runtime.py` continues to expose the small task runtime ports and typed snapshot types.
  `rg -n "snapshot|Snapshot|status_map|status_mapping" src/swarmline/orchestration/coding_task_runtime.py` finds typed snapshot and status mapping logic.
  `rg -n "create_task|list_ready_tasks|mark_in_progress|mark_blocked|unblock_task|mark_completed|mark_failed|cancel_task|resume_task" src/swarmline/orchestration/default_coding_task_runtime.py` finds the full lifecycle surface.
  `src/swarmline/runtime/thin/coding_profile.py` contains the allow-list/policy expansion for persistent `todo_read`, `todo_write`, and task-facing coding tools.
  `src/swarmline/runtime/thin/coding_toolpack.py` contains coding-mode wiring to the persistent task/todo facade.
  `python -m pytest tests/unit/test_coding_toolpack.py -v` exits 0 after the Phase 08 changes, proving visible/executable builder parity remains aligned while the coding surface expands.
  `tests/integration/test_coding_task_persistence.py` proves typed snapshot persistence happens on create/transition and resume rehydrates derived semantics before status projection.
  `python -m pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_task_runtime_contract.py tests/unit/test_coding_task_runtime.py tests/integration/test_coding_task_persistence.py tests/integration/test_thin_coding_profile.py -v` exits 0.
  `ruff check src/swarmline/orchestration/coding_task_runtime.py src/swarmline/orchestration/default_coding_task_runtime.py src/swarmline/session/task_session_store.py src/swarmline/todo/tools.py src/swarmline/runtime/thin/coding_profile.py src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/thin/runtime.py` exits 0.
  `mypy src/swarmline/orchestration/coding_task_runtime.py src/swarmline/orchestration/default_coding_task_runtime.py src/swarmline/session/task_session_store.py src/swarmline/runtime/thin/coding_profile.py` exits 0.
</acceptance_criteria>
<action>
  1. Implement `DefaultCodingTaskRuntime` in `src/swarmline/orchestration/default_coding_task_runtime.py` as the single concrete task-runtime owner for coding mode, reusing `GraphTaskBoard` semantics instead of reimplementing lifecycle.
  2. Ensure the concrete implementation satisfies the small task-runtime ports together as one composite service and exposes the full required lifecycle surface: create, list-ready, in-progress, blocked-with-reason, unblock, completed, failed-with-reason, cancelled, resume-by-persisted-binding.
  3. Add a concrete status-mapping layer from existing graph-task statuses to the coding-task contract statuses required by the feature spec, and keep typed snapshot logic in the contract layer so `CTSK-04` is implemented as code, not only as tests.
  4. Make snapshot persistence explicit for derived statuses: persist a typed `CodingTaskSnapshot` on create and every transition, including failed reason and dependency-derived readiness, and on resume rehydrate snapshot semantics before projecting to the runtime-visible status.
  5. Modify `src/swarmline/session/task_session_store.py` only as needed to support typed resume-friendly binding access for coding runs.
  6. Reuse provider-backed logic from `src/swarmline/todo/tools.py` for coding-mode todo operations; do not preserve markdown shim as the production path.
  7. Modify `src/swarmline/runtime/thin/coding_profile.py`, `src/swarmline/runtime/thin/coding_toolpack.py`, and `src/swarmline/runtime/thin/runtime.py` together so the active coding path uses the persistent task/todo facade and the coding allow-list / visible tool surface / executable path expand in lockstep for `todo_read`, `todo_write`, and the task-facing runtime surface.
  8. Ensure missing provider, missing binding, or unsupported resume wiring raises explicit errors.
</action>
<verify>
  <automated>python -m pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_task_runtime_contract.py tests/unit/test_coding_task_runtime.py tests/integration/test_coding_task_persistence.py tests/integration/test_thin_coding_profile.py -v && ruff check src/swarmline/orchestration/coding_task_runtime.py src/swarmline/orchestration/default_coding_task_runtime.py src/swarmline/session/task_session_store.py src/swarmline/todo/tools.py src/swarmline/runtime/thin/coding_profile.py src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/thin/runtime.py && mypy src/swarmline/orchestration/coding_task_runtime.py src/swarmline/orchestration/default_coding_task_runtime.py src/swarmline/session/task_session_store.py src/swarmline/runtime/thin/coding_profile.py</automated>
</verify>
<done>
  The concrete implementation lands only after the contract-first seam and contract tests exist.
  Persistent task/todo/session semantics are on the active coding path, not only behind compatibility shims.
  Typed snapshots and status mapping are implemented, verified, and connected to restart/resume behavior, including derived-status rehydration.
  The Phase-07 coding allow-list contract is expanded together with the persistent task/todo/runtime surface, so visible tools, executable tools, and policy stay aligned.
  The full lifecycle surface is executable and verified on the persistent coding path, not merely implied by status mapping.
  All `CTSK-*` requirements are satisfied by code plus integration tests.
</done>
</task>
