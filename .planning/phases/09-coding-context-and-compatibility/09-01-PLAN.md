---
phase: 09-coding-context-and-compatibility
plan: "01"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "08-01"
files_modified:
  - "src/swarmline/context/coding_context_builder.py"
  - "src/swarmline/context/builder.py"
  - "src/swarmline/runtime/thin/coding_toolpack.py"
  - "src/swarmline/runtime/ports/thin.py"
  - "src/swarmline/runtime/thin/runtime.py"
  - "src/swarmline/runtime/thin/prompts.py"
  - "tests/unit/test_coding_context_builder.py"
  - "tests/unit/test_coding_alias_compatibility.py"
  - "tests/unit/test_coding_profile.py"
  - "tests/unit/test_coding_toolpack.py"
  - "tests/unit/test_coding_profile_wiring.py"
  - "tests/integration/test_thin_coding_profile.py"
  - "tests/integration/test_coding_task_persistence.py"
autonomous: true
requirements:
  - "CCTX-01"
  - "CCTX-02"
  - "COMP-01"
  - "COMP-02"
  - "COMP-03"
must_haves:
  truths:
    - "Coding context adds only the required slices and only when coding profile is active."
    - "Budget pressure produces deterministic omission/truncation and preserves continuity facts."
    - "Legacy alias exposure and execution go through the same coding-mode compatibility path as canonical tools."
    - "Unsupported alias/profile/wiring states fail fast with no silent fallback."
  artifacts:
    - path: src/swarmline/context/coding_context_builder.py
      provides: bounded coding-context slice assembly
      contains: coding context
    - path: src/swarmline/runtime/thin/coding_toolpack.py
      provides: compatibility map shared by visible tool surface and executable routing
      contains: alias|compat
    - path: tests/unit/test_coding_alias_compatibility.py
      provides: parity and fail-fast proof
      contains: test_legacy_alias_matches_canonical_behavior
    - path: tests/unit/test_coding_context_builder.py
      provides: RED proof for bounded and deterministic coding-context assembly
      contains: test_coding_context_budget_truncation_is_deterministic
    - path: tests/integration/test_thin_coding_profile.py
      provides: runtime-level alias smoke proof on the real coding-mode path
      contains: read_file|write_file|execute
    - path: tests/integration/test_coding_task_persistence.py
      provides: persistence continuity re-check after coding context/runtime changes
      contains: restart|resume|continuity
  key_links:
    - from: src/swarmline/context/coding_context_builder.py
      to: src/swarmline/runtime/thin/runtime.py
      via: coding-only context assembly path
      pattern: coding_context|context_builder
    - from: src/swarmline/context/coding_context_builder.py
      to: src/swarmline/session/task_session_store.py
      via: persisted task/session continuity facts feed coding-context compaction and resume-safe assembly
      pattern: session|task|continuity|resume
    - from: src/swarmline/runtime/thin/coding_toolpack.py
      to: src/swarmline/runtime/ports/thin.py
      via: alias ToolSpec exposure and executor parity in coding mode
      pattern: alias|compat|toolpack
    - from: src/swarmline/runtime/ports/thin.py
      to: src/swarmline/runtime/thin/runtime.py
      via: advertised tools and executable tools share one compatibility map
      pattern: active_tools|local_tools
---

# Plan 09-01: Coding Context Assembler and Compatibility/Fail-Fast Wiring

## Goal

Добавить bounded coding-specific context assembly и legacy alias compatibility без второй implementation path и без silent fallback.

## Tasks

<task id="9.1.1" type="auto" tdd="true" critical="yes">
<name>RED tests для coding context slices, budget discipline и alias parity</name>
<files>src/swarmline/context/coding_context_builder.py, src/swarmline/runtime/thin/coding_toolpack.py, src/swarmline/runtime/ports/thin.py, tests/unit/test_coding_context_builder.py, tests/unit/test_coding_alias_compatibility.py, tests/integration/test_thin_coding_profile.py</files>
<read_first>
  .planning/phases/09-coding-context-and-compatibility/09-CONTEXT.md
  .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
  src/swarmline/context/builder.py
  src/swarmline/runtime/thin/coding_toolpack.py
  src/swarmline/runtime/ports/thin.py
  src/swarmline/runtime/thin/runtime.py
  src/swarmline/runtime/thin/prompts.py
</read_first>
<acceptance_criteria>
  `tests/unit/test_coding_context_builder.py` exists and contains `def test_coding_context_budget_truncation_is_deterministic`.
  `tests/unit/test_coding_alias_compatibility.py` exists and contains `def test_legacy_alias_matches_canonical_behavior`.
  `tests/unit/test_coding_alias_compatibility.py` contains `def test_unsupported_alias_fails_fast`.
  `tests/integration/test_thin_coding_profile.py` contains RED-first runtime smoke proof for `read_file`, `write_file`, and `execute` on the coding-mode path.
  `python -m pytest tests/unit/test_coding_context_builder.py tests/unit/test_coding_alias_compatibility.py tests/integration/test_thin_coding_profile.py -v` exits non-zero before implementation and exits 0 after implementation.
</acceptance_criteria>
<action>
  1. Create `tests/unit/test_coding_context_builder.py` with RED-first tests for:
     - coding mode includes task, task board, workspace, recent search, session, skill/profile slices;
     - non-coding mode excludes those slices;
     - budget pressure causes deterministic truncation/omission;
     - compaction preserves continuity facts required for resume.
  2. Create `tests/unit/test_coding_alias_compatibility.py` with RED-first tests for:
     - `read_file`, `write_file`, `edit_file`, `execute`, `write_todos`, `task` resolve to canonical behavior when supported;
     - alias policy path and result semantics match canonical names;
     - unsupported alias/profile/wiring states fail fast;
     - compatibility layer does not silently fallback to a generic path.
  3. Extend `tests/integration/test_thin_coding_profile.py` or add equivalent integration coverage so Phase 09 has a RED-first runtime-level smoke proof for alias execution parity via `read_file`, `write_file`, and `execute` on the coding-mode path.
</action>
<verify>
  <automated>python -m pytest tests/unit/test_coding_context_builder.py tests/unit/test_coding_alias_compatibility.py tests/integration/test_thin_coding_profile.py -v</automated>
</verify>
<done>
  RED tests exist for slice presence, budget discipline, alias parity, runtime-level alias smoke proof, and fail-fast paths.
  The failing assertions point to missing coding-context and compatibility wiring rather than missing imports or syntax errors.
</done>
</task>

<task id="9.1.2" type="auto" tdd="true" critical="yes" depends="9.1.1">
<name>Implement coding context assembler and compatibility resolver on the real coding-mode surface</name>
<files>src/swarmline/context/coding_context_builder.py, src/swarmline/context/builder.py, src/swarmline/runtime/thin/coding_toolpack.py, src/swarmline/runtime/ports/thin.py, src/swarmline/runtime/thin/runtime.py, src/swarmline/runtime/thin/prompts.py, tests/unit/test_coding_context_builder.py, tests/unit/test_coding_alias_compatibility.py, tests/integration/test_thin_coding_profile.py, tests/integration/test_coding_task_persistence.py</files>
<read_first>
  src/swarmline/context/builder.py
  src/swarmline/runtime/thin/coding_toolpack.py
  src/swarmline/runtime/ports/thin.py
  src/swarmline/runtime/thin/runtime.py
  src/swarmline/runtime/thin/prompts.py
  .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
</read_first>
<acceptance_criteria>
  `src/swarmline/context/coding_context_builder.py` exists or `src/swarmline/context/builder.py` contains a dedicated coding-context assembly path.
  `src/swarmline/runtime/thin/runtime.py` references the coding context assembler only when coding profile is active.
  `src/swarmline/runtime/thin/prompts.py` contains wiring that preserves deterministic coding-context inclusion under budget pressure.
  `src/swarmline/runtime/thin/coding_toolpack.py` contains alias compatibility wiring for coding-mode tool exposure.
  `src/swarmline/runtime/ports/thin.py` references the same compatibility path used by the coding toolpack for advertised tools.
  `python -m pytest tests/unit/test_coding_context_builder.py tests/unit/test_coding_alias_compatibility.py -v` exits 0.
  `python -m pytest tests/integration/test_thin_coding_profile.py -v` exits 0 and includes smoke/integration proof for `read_file`, `write_file`, and `execute` on the real coding-mode path.
  `python -m pytest tests/integration/test_coding_task_persistence.py -v` exits 0 after the Phase 09 changes, proving the active coding path still preserves persistence/resume behavior from Phase 08.
  `python -m pytest tests/unit/test_thin_runtime.py tests/integration/test_thin_runtime_tools.py -v` exits 0 as the required broader regression for runtime/context/prompt changes.
  `python -m pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py -v` exits 0 as the required broader regression for coding-profile/tool-surface drift after touching `coding_toolpack.py` and `ports/thin.py`.
  `ruff check src/swarmline/context/ src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/ports/thin.py src/swarmline/runtime/thin/runtime.py src/swarmline/runtime/thin/prompts.py` exits 0.
  `mypy src/swarmline/context/ src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/ports/thin.py src/swarmline/runtime/thin/runtime.py` exits 0.
</acceptance_criteria>
<action>
  1. Create `src/swarmline/context/coding_context_builder.py` or extend `src/swarmline/context/builder.py` with a dedicated coding-context assembly path that adds only the required coding slices and explicitly consumes persisted task/session continuity facts needed for resume-safe compaction.
  2. Modify `src/swarmline/runtime/thin/runtime.py` and `src/swarmline/runtime/thin/prompts.py` so coding-mode context is wired through the bounded assembler and remains deterministic under budget pressure.
  3. Modify `src/swarmline/runtime/thin/coding_toolpack.py` and `src/swarmline/runtime/ports/thin.py` so alias ToolSpec exposure and executor resolution use the same compatibility map as the canonical coding-mode surface.
  4. Keep any `builtin_tools.py` changes compatibility-only; do not make it the primary alias execution path for coding mode.
  5. Ensure unsupported alias, unsupported profile, and broken compatibility wiring paths raise explicit failures with no silent downgrade.
  6. Add or extend runtime-level integration coverage in `tests/integration/test_thin_coding_profile.py` so legacy aliases are exercised on the real coding-mode path, not only through unit parity tests.
  7. Because this phase changes `context/builder.py`, `runtime.py`, `coding_toolpack.py`, `ports/thin.py`, and `prompts.py`, run the broader regression required by the feature spec and project rules: non-coding thin runtime regression, coding-profile/tool-surface regression, and persistence/resume regression for the active coding path.
</action>
<verify>
  <automated>python -m pytest tests/unit/test_coding_context_builder.py tests/unit/test_coding_alias_compatibility.py tests/integration/test_thin_coding_profile.py tests/integration/test_coding_task_persistence.py tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/unit/test_thin_runtime.py tests/integration/test_thin_runtime_tools.py -v && ruff check src/swarmline/context/ src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/ports/thin.py src/swarmline/runtime/thin/runtime.py src/swarmline/runtime/thin/prompts.py && mypy src/swarmline/context/ src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/ports/thin.py src/swarmline/runtime/thin/runtime.py</automated>
</verify>
<done>
  Coding context and alias compatibility are wired through the actual coding-mode surface, not through legacy-only shims.
  Budget discipline, alias smoke parity, persistence continuity, and broader non-coding regression are validated with executable tests and static checks.
  All `CCTX-*` and `COMP-*` requirements are satisfied by code plus tests.
</done>
</task>
