---
phase: 10-coding-subagent-inheritance-and-validation
plan: "01"
type: execute
wave: 4
depends_on:
  - "07-01"
  - "08-01"
  - "09-01"
files_modified:
  - "src/swarmline/orchestration/thin_subagent.py"
  - "src/swarmline/runtime/thin/subagent_tool.py"
  - "src/swarmline/runtime/thin/runtime.py"
  - "tests/integration/test_coding_subagent_inheritance.py"
  - "tests/integration/test_thin_coding_profile_regression.py"
  - ".planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md"
autonomous: true
requirements:
  - "CSUB-01"
  - "CSUB-02"
  - "CSUB-03"
  - "CVAL-01"
  - "CVAL-02"
  - "CVAL-03"
must_haves:
  truths:
    - "Child thin runs inherit the same coding profile, visible tool surface, executable behavior, policy, and task context lineage as the parent coding run."
    - "Incompatible inheritance state fails fast instead of degrading silently to generic thin mode."
    - "The full coding-agent tranche closes with targeted packs, broader regression, ruff, mypy, and an explicit PASS gate."
    - "Phase 10 introduces no new public interface, protocol, or public signature changes, proven by the Python AST public-surface audit."
  artifacts:
    - path: tests/integration/test_coding_subagent_inheritance.py
      provides: parent-child parity and fail-fast proof
      contains: test_parent_coding_profile_is_inherited_by_child
    - path: tests/integration/test_thin_coding_profile_regression.py
      provides: non-coding no-regression and critical-path tranche validation
      contains: test_non_coding_thin_run_remains_unchanged
    - path: .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md
      provides: final tranche verification gate artifact
      contains: Verdict: PASS
    - path: src/swarmline/runtime/thin/subagent_tool.py
      provides: runtime inheritance wiring
      contains: coding_profile
  key_links:
    - from: src/swarmline/runtime/thin/subagent_tool.py
      to: src/swarmline/runtime/thin/coding_toolpack.py
      via: child uses the same coding-mode tool lineage as the parent
      pattern: coding_profile|toolpack
    - from: src/swarmline/orchestration/thin_subagent.py
      to: src/swarmline/context/
      via: inherited child run keeps the same coding-context continuity sources as the parent
      pattern: context|continuity|resume
    - from: src/swarmline/runtime/thin/subagent_tool.py
      to: src/swarmline/policy/
      via: inherited child run preserves the parent policy surface and default-deny boundaries
      pattern: policy|allow|deny
    - from: src/swarmline/orchestration/thin_subagent.py
      to: src/swarmline/orchestration/coding_task_runtime.py
      via: task context and continuity facts propagate to child
      pattern: task|context
    - from: .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md
      to: .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
      via: explicit spec-vs-code-vs-tests-vs-DoD tranche gate closure
      pattern: Verdict: PASS|spec vs code vs tests vs DoD
---

# Plan 10-01: Coding-Profile Subagent Inheritance and Tranche-Level Validation

## Goal

Обеспечить корректное наследование coding-agent profile на thin subagents и закрыть tranche финальной validation/no-regression проверкой.

## Tasks

<task id="10.1.1" type="auto" tdd="true" critical="yes">
<name>RED tests для coding subagent inheritance, parity и non-coding no-regression</name>
<files>src/swarmline/orchestration/thin_subagent.py, src/swarmline/runtime/thin/subagent_tool.py, src/swarmline/runtime/thin/runtime.py, tests/integration/test_coding_subagent_inheritance.py, tests/integration/test_thin_coding_profile_regression.py, .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md</files>
<read_first>
  .planning/phases/10-coding-subagent-inheritance-and-validation/10-CONTEXT.md
  AGENTS.md
  RULES.md
  .planning/ROADMAP.md
  .planning/REQUIREMENTS.md
  .memory-bank/reports/2026-04-12_analysis_thin-coding-agent-reuse-aura-claw-pi-mono.md
  .memory-bank/plans/2026-04-12_feature_thin-coding-agent-profile.md
  .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
  src/swarmline/orchestration/thin_subagent.py
  src/swarmline/runtime/thin/subagent_tool.py
  src/swarmline/runtime/thin/runtime.py
  src/swarmline/runtime/thin/coding_toolpack.py
  src/swarmline/runtime/ports/thin.py
  src/swarmline/context/
  src/swarmline/policy/
</read_first>
<acceptance_criteria>
  `tests/integration/test_coding_subagent_inheritance.py` exists and contains `def test_parent_coding_profile_is_inherited_by_child`.
  `tests/integration/test_coding_subagent_inheritance.py` contains `def test_incompatible_inheritance_state_fails_fast`.
  `tests/integration/test_coding_subagent_inheritance.py` contains `def test_child_visible_and_executable_tools_match_parent_lineage`.
  `tests/integration/test_thin_coding_profile_regression.py` exists and contains `def test_non_coding_thin_run_remains_unchanged`.
  `python -m pytest tests/integration/test_coding_subagent_inheritance.py tests/integration/test_thin_coding_profile_regression.py -v` exits non-zero before implementation and exits 0 after implementation.
</acceptance_criteria>
<action>
  1. Create `tests/integration/test_coding_subagent_inheritance.py` with RED-first tests for:
     - parent coding run gives child the same coding profile, visible tool surface, executable behavior, and policy;
     - task context and continuity facts propagate to child;
     - incompatible inheritance state fails fast.
  2. Create `tests/integration/test_thin_coding_profile_regression.py` with RED-first tests for:
     - non-coding thin runs remain behaviorally unchanged;
     - coding-profile critical path works end-to-end;
     - tranche acceptance gate can be driven from concrete targeted and broader regression commands.
</action>
<verify>
  <automated>python -m pytest tests/integration/test_coding_subagent_inheritance.py tests/integration/test_thin_coding_profile_regression.py -v</automated>
</verify>
<done>
  RED integration tests exist for inheritance parity, incompatible-state fail-fast, and non-coding no-regression.
  The failing assertions point to missing inheritance and tranche-validation wiring rather than missing imports or syntax errors.
</done>
</task>

<task id="10.1.2" type="auto" tdd="true" critical="yes" depends="10.1.1">
<name>Implement inheritance wiring and operationalize final tranche acceptance gate</name>
<files>src/swarmline/orchestration/thin_subagent.py, src/swarmline/runtime/thin/subagent_tool.py, src/swarmline/runtime/thin/runtime.py, tests/integration/test_coding_subagent_inheritance.py, tests/integration/test_thin_coding_profile_regression.py, .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md</files>
<read_first>
  .planning/phases/10-coding-subagent-inheritance-and-validation/10-CONTEXT.md
  AGENTS.md
  RULES.md
  .planning/ROADMAP.md
  .planning/REQUIREMENTS.md
  .memory-bank/reports/2026-04-12_analysis_thin-coding-agent-reuse-aura-claw-pi-mono.md
  .memory-bank/plans/2026-04-12_feature_thin-coding-agent-profile.md
  src/swarmline/orchestration/thin_subagent.py
  src/swarmline/runtime/thin/subagent_tool.py
  src/swarmline/runtime/thin/runtime.py
  src/swarmline/runtime/thin/coding_toolpack.py
  src/swarmline/runtime/ports/thin.py
  src/swarmline/orchestration/coding_task_runtime.py
  src/swarmline/context/
  src/swarmline/policy/
  .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
</read_first>
<acceptance_criteria>
  `src/swarmline/orchestration/thin_subagent.py` or `src/swarmline/runtime/thin/subagent_tool.py` contains coding-profile inheritance wiring.
  `python -m pytest tests/integration/test_coding_subagent_inheritance.py tests/integration/test_thin_coding_profile_regression.py -v` exits 0.
  `test -f .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md` exits 0.
  A Python AST audit comparing `HEAD` vs working tree for `thin_subagent.py`, `subagent_tool.py`, and `runtime.py` proves there are no public interface/protocol/signature changes in Phase 10.
  `rg -n "Targeted Verification|Broader Regression|Static Checks|CVAL-03 Interface Proof|LLM-as-Judge|spec vs code vs tests vs DoD|Verdict: PASS" .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md` exits 0.
  `ruff check src/ tests/` exits 0.
  `mypy src/swarmline/` exits 0.
  The explicit targeted pack, broader regression, and final `PASS` verdict artifact are present for this phase.
</acceptance_criteria>
<action>
  1. Modify `src/swarmline/orchestration/thin_subagent.py` and `src/swarmline/runtime/thin/subagent_tool.py` so child thin runs inherit coding profile, the same visible tool surface and executable behavior lineage from `coding_toolpack.py`, the same policy surface, and the same task-context continuity facts as the parent coding run.
  2. Modify `src/swarmline/runtime/thin/runtime.py` only as needed to support inheritance wiring without changing non-coding thin behavior.
  3. Add explicit fail-fast handling for incompatible inheritance state; do not silently downgrade to generic thin mode.
  4. Hard-lock `CVAL-03` for Phase 10: this phase must not introduce or modify any public interface, protocol declaration, or public signature. If such a change is required, stop execution and replan that work into an earlier phase with explicit contract-first seams and tests.
  5. Record the `CVAL-03 Interface Proof` in `.planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md` as `No public interface/signature changes in Phase 10`, backed by a Python AST audit that compares `HEAD` vs working tree for `thin_subagent.py`, `subagent_tool.py`, and `runtime.py` and checks:
     - top-level public classes;
     - top-level public functions / async functions;
     - public method signatures on public classes;
     - any `Protocol` / `@runtime_checkable` declarations.
  6. Run and close tranche validation with concrete commands:
     - `python -m pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/unit/test_coding_task_runtime.py tests/integration/test_coding_task_persistence.py tests/unit/test_coding_context_builder.py tests/unit/test_coding_alias_compatibility.py tests/integration/test_coding_subagent_inheritance.py tests/integration/test_thin_coding_profile_regression.py -v`
     - `python -m pytest tests/unit/test_thin_runtime.py tests/integration/test_thin_runtime_tools.py tests/integration/test_policy_chain.py tests/integration/test_session_lifecycle.py tests/integration/test_context_assembly.py tests/integration/test_thin_subagent_e2e.py -v`
     - `ruff check src/ tests/`
     - `mypy src/swarmline/`
     - run a final LLM-as-Judge pass over the tranche outputs and include the result in the gate artifact;
     - write `.planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md` with sections `Targeted Verification`, `Broader Regression`, `Static Checks`, `CVAL-03 Interface Proof`, `LLM-as-Judge`, `spec vs code vs tests vs DoD`, and final line `Verdict: PASS`
</action>
<verify>
  <automated>python -m pytest tests/integration/test_coding_subagent_inheritance.py tests/integration/test_thin_coding_profile_regression.py -v && python -m pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/unit/test_coding_task_runtime.py tests/integration/test_coding_task_persistence.py tests/unit/test_coding_context_builder.py tests/unit/test_coding_alias_compatibility.py tests/unit/test_thin_runtime.py tests/integration/test_thin_runtime_tools.py tests/integration/test_policy_chain.py tests/integration/test_session_lifecycle.py tests/integration/test_context_assembly.py tests/integration/test_thin_subagent_e2e.py -v && test -f .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md && python - <<'PY'\nimport ast\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nFILES = [\n    \"src/swarmline/orchestration/thin_subagent.py\",\n    \"src/swarmline/runtime/thin/subagent_tool.py\",\n    \"src/swarmline/runtime/thin/runtime.py\",\n]\n\n\ndef signature_for_function(node):\n    args = [a.arg for a in node.args.posonlyargs + node.args.args]\n    if node.args.vararg:\n        args.append(\"*\" + node.args.vararg.arg)\n    if node.args.kwonlyargs:\n        args.extend(a.arg for a in node.args.kwonlyargs)\n    if node.args.kwarg:\n        args.append(\"**\" + node.args.kwarg.arg)\n    return tuple(args)\n\n\ndef public_surface(source):\n    tree = ast.parse(source)\n    surface = []\n    for node in tree.body:\n        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)) and not node.name.startswith(\"_\"):\n            surface.append((\"function\", node.name, signature_for_function(node)))\n        elif isinstance(node, ast.ClassDef) and not node.name.startswith(\"_\"):\n            bases = tuple(ast.unparse(base) for base in node.bases)\n            methods = []\n            for child in node.body:\n                if isinstance(child, (ast.FunctionDef, ast.AsyncFunctionDef)) and not child.name.startswith(\"_\"):\n                    methods.append((child.name, signature_for_function(child)))\n            decorators = tuple(ast.unparse(d) for d in node.decorator_list)\n            surface.append((\"class\", node.name, bases, tuple(methods), decorators))\n    return tuple(surface)\n\nfor rel in FILES:\n    current = Path(rel).read_text(encoding=\"utf-8\")\n    head = subprocess.check_output([\"git\", \"show\", f\"HEAD:{rel}\"], text=True)\n    if public_surface(current) != public_surface(head):\n        print(f\"PUBLIC_SURFACE_CHANGED:{rel}\")\n        sys.exit(1)\nprint(\"PUBLIC_SURFACE_UNCHANGED\")\nPY\n&& rg -n \"CVAL-03 Interface Proof|No public interface/signature changes in Phase 10\" .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md && ruff check src/ tests/ && mypy src/swarmline/ && rg -n \"Targeted Verification|Broader Regression|Static Checks|CVAL-03 Interface Proof|LLM-as-Judge|spec vs code vs tests vs DoD|Verdict: PASS\" .planning/phases/10-coding-subagent-inheritance-and-validation/10-01-GATE.md</automated>
</verify>
<done>
  Child inheritance uses the same coding-mode lineage as the parent for tools, policy, and task context.
  The tranche acceptance gate is operationalized as concrete commands plus a persisted gate artifact ending in an explicit PASS criterion.
  `CVAL-03` is satisfied by a hard no-interface-change rule for Phase 10, verified by the Python AST public-surface audit comparing `HEAD` vs working tree and recorded in the gate artifact.
</done>
</task>
