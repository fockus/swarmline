# Plan 14-01: Session Resume

Phase: 14
Plan: 01
Status: Ready
Created: 2026-04-13

## Tasks

<task id="1" name="JsonlMessageStore — JSONL file-based persistence" priority="high">
<action>
1. Read existing MessageStore protocol (src/swarmline/protocols/memory.py)
2. Read existing InMemoryMessageStore (src/swarmline/memory/inmemory.py) for patterns
3. Write tests FIRST in tests/unit/test_jsonl_store.py:
   - test_save_message_creates_jsonl_file
   - test_save_message_appends_to_existing
   - test_get_messages_returns_saved
   - test_get_messages_with_limit
   - test_get_messages_empty_file
   - test_get_messages_nonexistent_session
   - test_count_messages_accurate
   - test_delete_messages_before_keeps_last_n
   - test_roundtrip_all_message_types (user, assistant, tool, system)
   - test_roundtrip_tool_calls_json
   - test_roundtrip_metadata
   - test_concurrent_safe_append
4. Create src/swarmline/session/jsonl_store.py:
   - JsonlMessageStore(base_dir: str | Path)
   - One JSONL file per (user_id, topic_id): {base_dir}/{user_id}_{topic_id}.jsonl
   - save_message: append one JSON line
   - get_messages: read file, parse lines, return last N
   - count_messages: count lines
   - delete_messages_before: rewrite file keeping last N
5. Ensure MessageStore protocol compliance
</action>
<verify>
- python -m pytest tests/unit/test_jsonl_store.py -v (all pass)
- ruff check src/swarmline/session/jsonl_store.py (clean)
- All message types round-trip without data loss
</verify>
<done>
- JsonlMessageStore implements MessageStore protocol
- JSONL format: one JSON object per line
- 12+ unit tests covering all operations + edge cases
- File-based, no external dependencies
</done>
</task>

<task id="2" name="Conversation resume + auto-persist + auto-compact" priority="high">
<action>
1. Read src/swarmline/agent/conversation.py thoroughly
2. Read src/swarmline/compaction.py for CompactionConfig/ConversationCompactionFilter
3. Write tests FIRST in tests/unit/test_session_resume.py:
   - test_conversation_resume_loads_history
   - test_conversation_resume_empty_session
   - test_conversation_auto_persist_saves_messages
   - test_conversation_auto_persist_off_by_default
   - test_conversation_auto_compact_on_resume
   - test_conversation_auto_compact_skipped_under_threshold
   - test_conversation_resume_then_say_continues
   - test_conversation_resume_nonexistent_session_empty
4. Extend Conversation class:
   - Add message_store param to __init__
   - Add compaction_config param to __init__
   - resume(session_id) → load history from message_store
   - Auto-persist: after say(), save new messages to store
   - Auto-compact: on resume, if history exceeds threshold, compact
5. Ensure backward compat: no message_store = current behavior
</action>
<verify>
- python -m pytest tests/unit/test_session_resume.py -v (all pass)
- ruff check src/swarmline/agent/conversation.py (clean)
- Backward compatibility: existing conversation tests pass
</verify>
<done>
- Conversation.resume(session_id) loads history
- Auto-persist saves messages after each say()
- Auto-compact triggers when restored history too large
- 8+ unit tests
- All existing conversation tests pass
</done>
</task>

<task id="3" name="Integration tests + exports" priority="medium">
<action>
1. Write tests in tests/integration/test_session_resume.py:
   - test_full_session_persist_and_resume_cycle
   - test_resume_with_auto_compaction
   - test_jsonl_store_with_conversation_end_to_end
   - test_resume_preserves_tool_call_results
   - test_multiple_sessions_isolated
   - test_backward_compat_no_store
2. Export JsonlMessageStore from __init__.py
3. Run full test suite
</action>
<verify>
- python -m pytest tests/integration/test_session_resume.py -v (all pass)
- python -m pytest tests/ -x -q --no-header (full suite)
- ruff check src/ tests/ (clean)
</verify>
<done>
- 6+ integration tests
- Full test suite green
- JsonlMessageStore exported
</done>
</task>

## Dependencies

```
Task 1 (JsonlStore) ─┐
                      ├─→ Task 3 (Integration)
Task 2 (Conversation)─┘
```

Tasks 1 and 2 are independent. Task 3 depends on both.

## Scope: S-scope (3 tasks)
