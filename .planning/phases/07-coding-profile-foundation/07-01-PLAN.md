---
phase: 07-coding-profile-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/swarmline/runtime/thin/coding_profile.py
  - src/swarmline/runtime/thin/coding_toolpack.py
  - src/swarmline/runtime/thin/runtime.py
  - src/swarmline/runtime/ports/thin.py
  - src/swarmline/runtime/thin/builtin_tools.py
  - src/swarmline/agent/config.py
  - src/swarmline/agent/runtime_wiring.py
  - tests/unit/test_coding_profile.py
  - tests/unit/test_coding_toolpack.py
  - tests/unit/test_coding_profile_wiring.py
  - tests/integration/test_thin_coding_profile.py
autonomous: true
requirements:
  - CADG-01
  - CADG-02
  - CADG-03
  - CADG-04
  - CADG-05
must_haves:
  truths:
    - "ThinRuntime accepts an explicit coding profile without a second runtime class."
    - "Coding-mode visible tools and executable local tools share the same canonical names."
    - "Coding-mode read/write/edit/multi_edit/bash/ls/glob/grep come from shared builtin factories."
    - "Default-deny behavior outside coding mode remains unchanged."
    - "Coding mode allows only the declared allow-list and fails fast on drift."
  artifacts:
    - path: src/swarmline/runtime/thin/coding_profile.py
      provides: typed coding-profile contract and allow-list constants
      contains: CodingProfileConfig
    - path: src/swarmline/runtime/thin/coding_toolpack.py
      provides: one builder for ToolSpec output plus executable local tools
      contains: build_coding_toolpack
    - path: tests/integration/test_thin_coding_profile.py
      provides: integration coverage for opt-in profile, policy scope, and non-coding regression
      contains: test_thin_runtime_
  key_links:
    - from: src/swarmline/agent/runtime_wiring.py
      to: src/swarmline/runtime/thin/runtime.py
      via: create_kwargs["coding_profile"]
      pattern: coding_profile
    - from: src/swarmline/runtime/thin/coding_toolpack.py
      to: src/swarmline/runtime/thin/runtime.py
      via: builder result merged into local executors and active tool surface
      pattern: build_coding_toolpack|coding_toolpack
    - from: src/swarmline/runtime/thin/coding_profile.py
      to: src/swarmline/policy/tool_policy.py
      via: allowed_system_tools or equivalent allow-list wiring
      pattern: allowed_system_tools|allowed_tool_names
---

<objective>
Implement the Phase 07 foundation as one opt-in coding profile on top of the existing `ThinRuntime`.

Purpose: satisfy `CADG-01..05` without a new runtime hierarchy, without new dependencies, and without changing secure-by-default behavior for non-coding runs.
Output: typed coding-profile contracts, one canonical coding tool-pack builder, ThinRuntime/ThinRuntimePort wiring, and targeted regressions proving policy scope plus visible/executable parity.
</objective>

<execution_context>
@/Users/fockus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/fockus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-coding-profile-foundation/07-CONTEXT.md
@.specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
@.memory-bank/reports/2026-04-12_analysis_thin-coding-agent-reuse-aura-claw-pi-mono.md
@.memory-bank/plans/2026-04-12_feature_thin-coding-agent-profile.md
@src/swarmline/tools/builtin.py
@src/swarmline/todo/tools.py
@src/swarmline/runtime/thin/runtime.py
@src/swarmline/runtime/thin/builtin_tools.py
@src/swarmline/runtime/ports/thin.py
@src/swarmline/policy/tool_policy.py
@src/swarmline/agent/config.py
@src/swarmline/agent/runtime_wiring.py
@tests/unit/test_thin_builtin_tools.py
@tests/unit/test_thin_builtin_tools_integration.py
@tests/unit/test_tool_policy.py

<interfaces>
- `AgentConfig` already carries portable-runtime knobs (`tool_policy`, `feature_mode`, `allow_native_features`, `native_config`) and must stay backward-compatible with optional defaults.
- `RuntimeConfig` already validates `feature_mode`; Phase 07 should flow through `create_kwargs`, not a new runtime name.
- `DefaultToolPolicy` already supports `allowed_system_tools`; use that seam for coding mode instead of weakening `ALWAYS_DENIED_TOOLS`.
- `create_sandbox_tools()` in `src/swarmline/tools/builtin.py` is the canonical source for file/shell/search executors and schemas.
- `create_todo_tools()` in `src/swarmline/todo/tools.py` is the canonical source for todo tool schemas/executors; do not revive `.todos.md` shim behavior in the active coding path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define coding-profile contracts and write RED tests</name>
  <files>src/swarmline/runtime/thin/coding_profile.py, src/swarmline/runtime/thin/coding_toolpack.py, tests/unit/test_coding_profile.py, tests/unit/test_coding_toolpack.py, tests/unit/test_coding_profile_wiring.py</files>
  <read_first>
    .planning/phases/07-coding-profile-foundation/07-CONTEXT.md
    .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
    .memory-bank/reports/2026-04-12_analysis_thin-coding-agent-reuse-aura-claw-pi-mono.md
    .memory-bank/plans/2026-04-12_feature_thin-coding-agent-profile.md
    src/swarmline/tools/builtin.py
    src/swarmline/todo/tools.py
    src/swarmline/runtime/thin/runtime.py
    src/swarmline/runtime/thin/builtin_tools.py
    src/swarmline/runtime/ports/thin.py
    src/swarmline/policy/tool_policy.py
    src/swarmline/agent/config.py
    src/swarmline/agent/runtime_wiring.py
    tests/unit/test_thin_builtin_tools.py
    tests/unit/test_thin_builtin_tools_integration.py
    tests/unit/test_tool_policy.py
    src/swarmline/runtime/thin/coding_profile.py
    src/swarmline/runtime/thin/coding_toolpack.py
    tests/unit/test_coding_profile.py
    tests/unit/test_coding_toolpack.py
    tests/unit/test_coding_profile_wiring.py
  </read_first>
  <acceptance_criteria>
    `rg -n "CodingProfileConfig|canonical_tool_names|allowed_tool_names" src/swarmline/runtime/thin/coding_profile.py` finds the typed contract.
    `rg -n "build_coding_toolpack|CodingToolPack" src/swarmline/runtime/thin/coding_toolpack.py` finds the canonical builder seam.
    `pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py -v` fails in RED because runtime wiring is not implemented yet.
  </acceptance_criteria>
  <action>
    Create the typed foundation before any runtime wiring:

    1. Add `src/swarmline/runtime/thin/coding_profile.py` with a frozen `CodingProfileConfig` dataclass and small helper constants/properties for the coding allow-list. The profile must be opt-in, validate only the supported profile name, and expose canonical coding-tool names for policy wiring. Keep the seam narrow: no more than 5 public methods total across new Protocols/classes. Do not implement Phase 8 persistence or Phase 9 context assembly here; only add typed contract hooks if absolutely needed.

    2. Add `src/swarmline/runtime/thin/coding_toolpack.py` with a typed `CodingToolPack` result and a `build_coding_toolpack(...)` seam that is intended to assemble both visible `ToolSpec` values and executable tool callables from shared factories. The contract must make parity observable (`tool_specs` names must be comparable to executor keys) and fail fast when required dependencies for coding mode are missing.

    3. Write RED-first tests:
       - `tests/unit/test_coding_profile.py` covers opt-in validation, unknown profile fail-fast, and canonical allow-list contents for `read/write/edit/multi_edit/bash/ls/glob/grep`.
       - `tests/unit/test_coding_toolpack.py` covers the contract that coding-mode specs and executors come from one builder, preserve name parity, and reject stub-only todo/task leakage.
       - `tests/unit/test_coding_profile_wiring.py` covers the intended `AgentConfig -> build_portable_runtime_plan -> ThinRuntime` wiring shape, including explicit policy scope for coding mode and no-profile backward compatibility.

    4. Keep tests intentionally RED at the end of this task. The new contract files may contain only the minimum typed scaffolding needed for imports, but do not implement the runtime behavior until Task 2.
  </action>
  <verify>
    <automated>pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py -v</automated>
  </verify>
  <done>
    Coding profile and tool-pack contracts exist as importable typed seams.
    RED tests exist for profile validation, canonical builder parity, and planned wiring behavior.
    The failing assertions point to missing implementation rather than syntax/import errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the canonical coding tool pack and ThinRuntime profile wiring</name>
  <files>src/swarmline/runtime/thin/coding_profile.py, src/swarmline/runtime/thin/coding_toolpack.py, src/swarmline/runtime/thin/runtime.py, src/swarmline/runtime/ports/thin.py, src/swarmline/runtime/thin/builtin_tools.py, src/swarmline/agent/config.py, src/swarmline/agent/runtime_wiring.py, tests/unit/test_coding_profile.py, tests/unit/test_coding_toolpack.py, tests/unit/test_coding_profile_wiring.py</files>
  <read_first>
    .planning/phases/07-coding-profile-foundation/07-CONTEXT.md
    .specs/tasks/todo/implement-thin-coding-agent-profile.feature.md
    src/swarmline/tools/builtin.py
    src/swarmline/todo/tools.py
    src/swarmline/runtime/thin/runtime.py
    src/swarmline/runtime/ports/thin.py
    src/swarmline/runtime/thin/builtin_tools.py
    src/swarmline/policy/tool_policy.py
    src/swarmline/agent/config.py
    src/swarmline/agent/runtime_wiring.py
    src/swarmline/runtime/thin/coding_profile.py
    src/swarmline/runtime/thin/coding_toolpack.py
    tests/unit/test_coding_profile.py
    tests/unit/test_coding_toolpack.py
    tests/unit/test_coding_profile_wiring.py
  </read_first>
  <acceptance_criteria>
    `rg -n "coding_profile" src/swarmline/agent/config.py src/swarmline/agent/runtime_wiring.py src/swarmline/runtime/thin/runtime.py src/swarmline/runtime/ports/thin.py` shows end-to-end opt-in wiring.
    `rg -n "create_sandbox_tools|create_todo_tools" src/swarmline/runtime/thin/coding_toolpack.py` proves the coding tool pack reuses shared factories.
    `rg -n "\\.todos\\.md|tracked" src/swarmline/runtime/thin/builtin_tools.py` does not show old stub behavior in the active coding-path logic.
    `pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py -v` passes.
  </acceptance_criteria>
  <action>
    Implement the foundation behavior without widening scope beyond Phase 07:

    1. Finalize `CodingProfileConfig` so coding mode is an explicit opt-in on the existing runtime path. Add `coding_profile: CodingProfileConfig | None = None` to `AgentConfig` with a `None` default, and propagate it through `build_portable_runtime_plan()` using `create_kwargs`. Do not add a new runtime name or a second runtime class.

    2. Implement `build_coding_toolpack(...)` so it is the only coding-mode builder for both visible `ToolSpec` values and executable local-tool callables. Source `read/write/edit/multi_edit/bash/ls/glob/grep` from `create_sandbox_tools()` and only source todo tools from `create_todo_tools()` when the profile is configured with the required provider hook. If coding mode requests tooling without the required provider or sandbox, raise an explicit error instead of reviving stub behavior.

    3. Update `ThinRuntime` and `ThinRuntimePort` to use the coding tool-pack builder when `coding_profile` is present. In coding mode, the builder must drive the model-visible tool surface and the executable tool registry from the same result so the names match exactly. Keep non-coding behavior backward-compatible.

    4. Make `builtin_tools.py` compatibility-only for legacy thin builtins. The active coding path must no longer rely on `write_todos -> .todos.md` or the noop `task` executor. If legacy names remain reachable, they must delegate into canonical implementations or fail fast explicitly; do not keep the stubbed semantics alive behind the coding profile.

    5. Wire policy scope through the existing `DefaultToolPolicy(allowed_system_tools=...)` seam. Coding mode must explicitly allow only the canonical coding tool names declared by the profile; non-coding mode must keep the existing default-deny baseline unchanged. Reject collisions where user-supplied local tools try to shadow canonical coding tool names.

    6. Bring the RED tests from Task 1 to GREEN with the smallest clean-architecture slice. No new dependencies, no persistence engine, no context-assembler implementation, and no subagent inheritance work in this task.
  </action>
  <verify>
    <automated>pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py -v && ruff check src/swarmline/runtime/thin/coding_profile.py src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/thin/runtime.py src/swarmline/runtime/ports/thin.py src/swarmline/runtime/thin/builtin_tools.py src/swarmline/agent/config.py src/swarmline/agent/runtime_wiring.py && mypy src/swarmline/runtime/thin/coding_profile.py src/swarmline/runtime/thin/coding_toolpack.py src/swarmline/runtime/thin/runtime.py src/swarmline/runtime/ports/thin.py src/swarmline/agent/config.py src/swarmline/agent/runtime_wiring.py</automated>
  </verify>
  <done>
    ThinRuntime accepts an optional coding profile without a new runtime hierarchy.
    Coding-mode visible tools and executable local tools come from one canonical builder and have matching names.
    Shared builtin factories back the canonical coding tools, and stubbed thin todo/task behavior is not part of the active coding path.
    Coding-mode policy is an explicit allow-list on top of the unchanged default-deny baseline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add integration regressions for policy scope, parity, and non-coding safety</name>
  <files>tests/integration/test_thin_coding_profile.py, tests/unit/test_thin_builtin_tools.py, tests/unit/test_thin_builtin_tools_integration.py, tests/unit/test_tool_policy.py</files>
  <read_first>
    .planning/phases/07-coding-profile-foundation/07-CONTEXT.md
    src/swarmline/runtime/thin/coding_profile.py
    src/swarmline/runtime/thin/coding_toolpack.py
    src/swarmline/runtime/thin/runtime.py
    src/swarmline/runtime/ports/thin.py
    src/swarmline/policy/tool_policy.py
    tests/unit/test_coding_profile.py
    tests/unit/test_coding_toolpack.py
    tests/unit/test_coding_profile_wiring.py
    tests/unit/test_thin_builtin_tools.py
    tests/unit/test_thin_builtin_tools_integration.py
    tests/unit/test_tool_policy.py
    tests/integration/test_thin_runtime_tools.py
    tests/integration/test_thin_hooks_integration.py
    tests/integration/test_thin_coding_profile.py
  </read_first>
  <acceptance_criteria>
    `pytest tests/integration/test_thin_coding_profile.py -v` passes.
    `pytest tests/unit/test_thin_builtin_tools.py tests/unit/test_thin_builtin_tools_integration.py tests/unit/test_tool_policy.py -v` passes after updating expectations to the new coding-profile path.
    `pytest tests/integration/test_thin_runtime_tools.py tests/unit/test_thin_runtime.py -v` passes, demonstrating no non-coding regression.
  </acceptance_criteria>
  <action>
    Add the regression closure for Phase 07:

    1. Create `tests/integration/test_thin_coding_profile.py` with end-to-end scenarios for:
       - explicit coding-profile opt-in on `ThinRuntime`;
       - parity between visible tool names and executable tool names in coding mode;
       - coding-mode policy allowing only the declared coding tool surface;
       - fail-fast on unknown profile, missing required provider hook, or canonical-name shadowing.

    2. Update the existing thin builtin and policy tests only where the source of truth changed:
       - keep legacy thin builtin coverage for non-coding compatibility;
       - add assertions that the coding profile does not use `.todos.md` shim semantics;
       - add policy assertions proving default-deny behavior remains unchanged when no coding profile is active.

    3. Run the targeted regression bundle for Phase 07. Fix only issues caused by the coding-profile foundation slice; do not expand into deferred Phase 8, 9, or 10 work.
  </action>
  <verify>
    <automated>pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/unit/test_thin_builtin_tools.py tests/unit/test_thin_builtin_tools_integration.py tests/unit/test_tool_policy.py tests/unit/test_thin_runtime.py tests/integration/test_thin_coding_profile.py tests/integration/test_thin_runtime_tools.py -v && ruff check src/swarmline/runtime/thin src/swarmline/agent tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/integration/test_thin_coding_profile.py && mypy src/swarmline/runtime/thin src/swarmline/agent</automated>
  </verify>
  <done>
    Phase 07 has integration coverage for coding-profile opt-in, policy scope, and fail-fast behavior.
    Existing non-coding thin tests still pass with the coding profile absent.
    The phase closes with targeted pytest, ruff, and mypy verification on touched runtime and agent modules.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/unit/test_thin_builtin_tools.py tests/unit/test_thin_builtin_tools_integration.py tests/unit/test_tool_policy.py tests/unit/test_thin_runtime.py tests/integration/test_thin_coding_profile.py tests/integration/test_thin_runtime_tools.py -v`
- `ruff check src/swarmline/runtime/thin src/swarmline/agent tests/unit/test_coding_profile.py tests/unit/test_coding_toolpack.py tests/unit/test_coding_profile_wiring.py tests/integration/test_thin_coding_profile.py`
- `mypy src/swarmline/runtime/thin src/swarmline/agent`
</verification>

<success_criteria>
`CADG-01`: coding profile is an optional seam on `ThinRuntime`, not a new runtime class.
`CADG-02`: coding-mode visible tool names equal executable tool names from one canonical builder.
`CADG-03`: coding-mode file/shell/search tools are assembled from shared builtin factories.
`CADG-04`: non-coding thin runs keep existing default-deny behavior and tool surface.
`CADG-05`: coding mode explicitly permits only the declared coding allow-list and fails fast on leakage or wiring drift.
</success_criteria>

<output>
After completion, create `.planning/phases/07-coding-profile-foundation/07-01-SUMMARY.md`
</output>
